<Window x:Class="PrivateAdsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сервис частных объявлений" Height="700" Width="1200" WindowStartupLocation="CenterScreen"
        >
    <Grid Background="{StaticResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Шапка -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceColor}" Padding="20,15" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,2">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="12" Opacity="0.2" Direction="270"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Image Source="/Resources/vozvrat-tovara.png" Width="60" Margin="0,0,20,0" Grid.Column="0"/>
                <TextBlock Text="Сервис частных объявлений"
                          Style="{StaticResource HeaderTextBlock}"
                          Grid.Column="1"
                          HorizontalAlignment="Left"/>
                <Button x:Name="BtnLogin"
                       Content="Войти"
                       Width="120"
                       Height="40"
                       Grid.Column="2"
                       Style="{StaticResource PrimaryButton}"
                       Click="BtnLogin_Click"/>
            </Grid>
        </Border>

        <!-- Фильтры -->
        <Border Grid.Row="1"
                Background="{StaticResource SurfaceColor}"
                Padding="25,20"
                Margin="20,20,20,0">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="8" Opacity="0.1" Direction="270"/>
            </Border.Effect>
            <StackPanel>
                <!-- Заголовок фильтров -->
                <TextBlock Text="Фильтры поиска"
                          FontSize="16"
                          FontWeight="SemiBold"
                          Foreground="{StaticResource TextPrimaryColor}"
                          Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Контейнер фильтров -->
                    <WrapPanel Grid.Column="0" VerticalAlignment="Center">
                        <!-- Поиск -->
                        <StackPanel Margin="0,0,15,0">
                            <TextBlock Text="Поиск"
                                      FontSize="12"
                                      Foreground="{StaticResource TextSecondaryColor}"
                                      Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtSearch"
                                    Width="280"
                                    Height="38"
                                    Style="{StaticResource ModernTextBox}"
                                    Tag="Введите название..."/>
                        </StackPanel>

                        <!-- Город -->
                        <StackPanel Margin="0,0,15,0">
                            <TextBlock Text="Город"
                                      FontSize="12"
                                      Foreground="{StaticResource TextSecondaryColor}"
                                      Margin="0,0,0,5"/>
                            <ComboBox x:Name="CmbCity"
                                     Width="140"
                                     Height="38"
                                     Style="{StaticResource ModernComboBox}"
                                     DisplayMemberPath="CityName"/>
                        </StackPanel>

                        <!-- Категория -->
                        <StackPanel Margin="0,0,15,0">
                            <TextBlock Text="Категория"
                                      FontSize="12"
                                      Foreground="{StaticResource TextSecondaryColor}"
                                      Margin="0,0,0,5"/>
                            <ComboBox x:Name="CmbCategory"
                                     Width="150"
                                     Height="38"
                                     Style="{StaticResource ModernComboBox}"
                                     DisplayMemberPath="CategoryName"/>
                        </StackPanel>

                        <!-- Тип -->
                        <StackPanel Margin="0,0,15,0">
                            <TextBlock Text="Тип"
                                      FontSize="12"
                                      Foreground="{StaticResource TextSecondaryColor}"
                                      Margin="0,0,0,5"/>
                            <ComboBox x:Name="CmbType"
                                     Width="120"
                                     Height="38"
                                     Style="{StaticResource ModernComboBox}"
                                     DisplayMemberPath="TypeName"/>
                        </StackPanel>

                        <!-- Статус -->
                        <StackPanel Margin="0,0,0,0">
                            <TextBlock Text="Статус"
                                      FontSize="12"
                                      Foreground="{StaticResource TextSecondaryColor}"
                                      Margin="0,0,0,5"/>
                            <ComboBox x:Name="CmbStatus"
                                     Width="120"
                                     Height="38"
                                     Style="{StaticResource ModernComboBox}"
                                     DisplayMemberPath="StatusName"/>
                        </StackPanel>
                    </WrapPanel>

                    <!-- Кнопка поиска -->
                    <Button Grid.Column="1"
                           Content="Найти"
                           Width="120"
                           Height="38"
                           Margin="15,17,0,0"
                           VerticalAlignment="Bottom"
                           Style="{StaticResource PrimaryButton}"
                           Click="BtnSearch_Click"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Список объявлений -->
        <ListView x:Name="LvAds"
                 Grid.Row="2"
                 Margin="20"
                 Style="{StaticResource ModernListView}"
                 ItemsSource="{Binding}"
                 ItemContainerStyle="{StaticResource ModernListViewItem}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Фото" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Grid Width="100" Height="100">
                                    <Border CornerRadius="4" ClipToBounds="True">
                                        <Image Source="{Binding PhotoPath, FallbackValue=/Resources/icon-clothes.png}"
                                              Stretch="UniformToFill"/>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Заголовок" DisplayMemberBinding="{Binding Title}" Width="280"/>
                    <GridViewColumn Header="Цена" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Price, StringFormat={}{0:N0} ₽}"
                                          FontWeight="SemiBold"
                                          Foreground="{StaticResource SuccessColor}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Город" DisplayMemberBinding="{Binding City.CityName}" Width="120"/>
                    <GridViewColumn Header="Категория" DisplayMemberBinding="{Binding Category.CategoryName}" Width="140"/>
                    <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding PostDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>