<Window x:Class="WpfApp6.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Симплекс-метод - MatModelUP" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#34495E" Margin="0,0,0,10">
            <TextBlock Text="Переменных:" Foreground="White" VerticalAlignment="Center"
                      FontSize="14" Margin="15,0,8,0"/>
            <TextBox x:Name="txtVariables" Width="60" Height="32" Text="2"
                    FontSize="14" VerticalContentAlignment="Center" TextAlignment="Center"
                    BorderBrush="#7F8C8D" BorderThickness="1"/>

            <TextBlock Text="Ограничений:" Foreground="White" VerticalAlignment="Center"
                      FontSize="14" Margin="25,0,8,0"/>
            <TextBox x:Name="txtConstraints" Width="60" Height="32" Text="3"
                    FontSize="14" VerticalContentAlignment="Center" TextAlignment="Center"
                    BorderBrush="#7F8C8D" BorderThickness="1"/>

            <Button x:Name="btnCreateSimplex" Content="Создать задачу"
                    Width="150" Height="36" Margin="25,0,0,0" Click="BtnCreateSimplex_Click"
                    Background="#3498DB" Foreground="White" FontWeight="SemiBold" FontSize="13"
                    BorderThickness="0" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#3498DB"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <RadioButton x:Name="rbMax" Content="MAX" Foreground="White"
                        VerticalAlignment="Center" Margin="35,0,15,0" FontWeight="SemiBold"
                        FontSize="13" GroupName="OptType" IsChecked="True"/>
            <RadioButton x:Name="rbMin" Content="MIN" Foreground="White"
                        VerticalAlignment="Center" FontWeight="SemiBold"
                        FontSize="13" GroupName="OptType"/>
        </StackPanel>

        <!-- Основное содержимое -->
        <Grid Grid.Row="1" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <Border Grid.Row="0" Background="#ECF0F1" Padding="10" Margin="0,0,0,15"
                    CornerRadius="4" BorderBrush="#BDC3C7" BorderThickness="1">
                <TextBlock x:Name="txtTitleSimplex"
                           Text="Задача линейного программирования - Симплекс-метод"
                           FontSize="18" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Foreground="#2C3E50"/>
            </Border>

            <!-- Область для таблиц -->
            <Border Grid.Row="1" Background="White" BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="4">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                             Padding="10">
                    <StackPanel x:Name="pnlSimplexContent" Orientation="Vertical"/>
                </ScrollViewer>
            </Border>

            <!-- Кнопки -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Center">
                <Button x:Name="btnSolveSimplex" Content="Решить симплекс-методом" Width="220" Height="42"
                        Click="BtnSolveSimplex_Click" IsEnabled="False"
                        Background="#27AE60" Foreground="White" FontSize="14" FontWeight="SemiBold"
                        BorderThickness="0" Margin="0,0,10,0" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#27AE60"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#229954"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#95A5A6"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button x:Name="btnResetSimplex" Content="Сбросить" Width="150" Height="42"
                        Click="BtnResetSimplex_Click"
                        Background="#E74C3C" Foreground="White" FontSize="14" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#E74C3C"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#C0392B"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>