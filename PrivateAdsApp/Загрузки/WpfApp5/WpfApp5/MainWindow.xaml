<Window x:Class="WpfTransportSolver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Транспортная Задача (Выбор Метода)" 
        Height="800" Width="1400"
        Background="#F0F2F5">

    <Window.Resources>
        <!-- Стиль для DataGrid (Excel-подобный) -->
        <Style x:Key="ExcelDataGrid" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="HeadersVisibility" Value="All"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#D0D0D0"/>
            <Setter Property="VerticalGridLinesBrush" Value="#D0D0D0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="ExcelColumnHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#4472C4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#2E5C9A"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ExcelRowHeader" TargetType="DataGridRowHeader">
            <Setter Property="Background" Value="#4472C4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="BorderBrush" Value="#2E5C9A"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <Style x:Key="ExcelCell" TargetType="DataGridCell">
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="#DCDFE4" BorderThickness="1" CornerRadius="8" Background="White" Padding="20" Margin="0,0,20,0">
            <StackPanel>

                <TextBlock Text="⚙️ Настройки и Ввод" 
                           FontSize="24" FontWeight="Bold" Foreground="#34495E" Margin="0,0,0,15"/>

                <TextBlock Text="1. Матрица Стоимости Cij:" 
                           FontWeight="SemiBold" Margin="0,5,0,5" Foreground="#34495E"/>
                <TextBox x:Name="CostMatrixInput" 
                         AcceptsReturn="True" 
                         Height="100" 
                         Text="24,50,55,27,16&#x0a;50,47,23,17,21&#x0a;35,59,55,27,41" 
                         FontFamily="Consolas" FontSize="14" 
                         BorderBrush="#3498DB" BorderThickness="1" Padding="5" Margin="0,0,0,10"/>

                <Grid Margin="0,10,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="2. Запасы Ai (Предложение):" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#27AE60"/>
                        <TextBox x:Name="SuppliesInput" 
                                 AcceptsReturn="True"
                                 Height="80"
                                 Text="200&#x0a;350&#x0a;300" 
                                 FontFamily="Consolas" FontSize="14"
                                 BorderBrush="#2ECC71" BorderThickness="1" Padding="5"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="3. Потребности Bj (Спрос):" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#C0392B"/>
                        <TextBox x:Name="DemandsInput" 
                                 AcceptsReturn="True"
                                 Height="80"
                                 Text="270&#x0a;130&#x0a;190&#x0a;150&#x0a;110" 
                                 FontFamily="Consolas" FontSize="14"
                                 BorderBrush="#E74C3C" BorderThickness="1" Padding="5"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="4. Выберите метод решения:" 
                           FontWeight="SemiBold" Margin="0,15,0,10" Foreground="#34495E"/>
                <StackPanel Margin="10,0,0,0">
                    <RadioButton x:Name="RadioNorthWest" Content="Метод Северо-Западного Угла" GroupName="Method" IsChecked="True" FontSize="14" Margin="0,3,0,3"/>
                    <RadioButton x:Name="RadioLeastCost" Content="Метод Минимального Элемента" GroupName="Method" FontSize="14" Margin="0,3,0,3"/>
                    <RadioButton x:Name="RadioPotentials" Content="Метод Потенциалов (Оптимизация)" GroupName="Method" FontSize="14" Margin="0,3,0,3"/>
                </StackPanel>

                <Button Content="🚀 РЕШИТЬ ЗАДАЧУ" 
                        Click="SolveButton_Click" 
                        Margin="0,25,0,0" 
                        Padding="12" 
                        FontSize="16" 
                        Background="#E67E22" 
                        Foreground="White" 
                        FontWeight="Bold" 
                        BorderThickness="0" />

                <TextBlock x:Name="StatusTextBlock" Margin="0,15,0,0" Foreground="#E74C3C" FontSize="14" TextWrapping="Wrap"/>

            </StackPanel>
        </Border>

        <Border Grid.Column="1" BorderBrush="#DCDFE4" BorderThickness="1" CornerRadius="8" Background="#ECF0F1" Padding="20">
            <StackPanel>
                <TextBlock x:Name="ResultHeader"
                           Text="🔍 Результат Решения" 
                           FontSize="24" FontWeight="Bold" Foreground="#34495E" Margin="0,0,0,15"/>

                <ScrollViewer VerticalScrollBarVisibility="Auto" Height="650">
                    <StackPanel x:Name="ResultPanel">
                        <Border BorderBrush="#BDC3C7" BorderThickness="1" Padding="15" Background="#FFFFFF" CornerRadius="5">
                            <TextBlock Text="Введите данные и нажмите 'Решить', чтобы увидеть результат." 
                                       FontFamily="Segoe UI" 
                                       FontSize="14" 
                                       Foreground="#7F8C8D"
                                       TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </StackPanel>
        </Border>
    </Grid>
</Window>