<Window x:Class="AdsService.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Сервис частных объявлений" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        FontFamily="Tw Cen MT"
        Background="White">
    
    <Window.Resources>
        <!-- Стиль для заголовков -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#37474F"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <!-- Стиль для обычного текста -->
        <Style x:Key="NormalText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#37474F"/>
        </Style>
        
        <!-- Стиль для дополнительного текста -->
        <Style x:Key="SecondaryText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="#78909C"/>
        </Style>
        
        <!-- Стиль для кнопок -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#FF9C1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Margin" Value="16,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E68900"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Стиль для вторичных кнопок -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#78909C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#546E7A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Стиль для текстовых полей -->
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderBrush" Value="#78909C"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <!-- Стиль для ComboBox -->
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderBrush" Value="#78909C"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок с логотипом -->
        <Border Grid.Row="0" Background="#37474F">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Логотип и название -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                    <Ellipse Width="50" Height="50" Fill="#FF9C1A" Margin="0,0,10,0"/>
                    <TextBlock Text="📢" FontSize="24" Foreground="White" 
                             VerticalAlignment="Center" HorizontalAlignment="Center"
                             Margin="-40,0,20,0"/>
                    <TextBlock Text="Сервис объявлений" FontSize="24" 
                             FontWeight="Bold" Foreground="White" 
                             VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Информация о пользователе -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" 
                          VerticalAlignment="Center" Margin="20,0">
                    <TextBlock Name="txtUserInfo" Text="Гость" 
                             Foreground="White" FontSize="14" 
                             VerticalAlignment="Center" Margin="0,0,20,0"/>
                    <Button Name="btnLogin" Content="Войти" 
                          Style="{StaticResource PrimaryButton}"
                          Width="100" Click="btnLogin_Click"/>
                    <Button Name="btnLogout" Content="Выйти" 
                          Style="{StaticResource SecondaryButton}"
                          Width="100" Visibility="Collapsed" 
                          Click="btnLogout_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Основное содержимое -->
        <TabControl Grid.Row="1" Name="mainTabControl" Margin="10">
            <!-- Вкладка: Все объявления -->
            <TabItem Header="Все объявления">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Панель поиска и фильтров -->
                    <GroupBox Grid.Row="0" Header="Поиск и фильтры" Margin="10">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBox Grid.Column="0" Name="txtSearch" 
                                   Style="{StaticResource TextBoxStyle}"
                                   Tag="Поиск по ключевым словам..." Margin="0,0,10,0"/>
                            
                            <ComboBox Grid.Column="1" Name="cmbCity" 
                                    Style="{StaticResource ComboBoxStyle}"
                                    DisplayMemberPath="." Margin="0,0,10,0"/>
                            
                            <ComboBox Grid.Column="2" Name="cmbCategory" 
                                    Style="{StaticResource ComboBoxStyle}"
                                    DisplayMemberPath="CategoryName" 
                                    SelectedValuePath="CategoryId" Margin="0,0,10,0"/>
                            
                            <ComboBox Grid.Column="3" Name="cmbType" 
                                    Style="{StaticResource ComboBoxStyle}"
                                    DisplayMemberPath="TypeName" 
                                    SelectedValuePath="TypeId" Margin="0,0,10,0"/>
                            
                            <StackPanel Grid.Column="4" Orientation="Horizontal">
                                <Button Name="btnSearch" Content="🔍 Найти" 
                                      Style="{StaticResource PrimaryButton}"
                                      Width="100" Click="btnSearch_Click"/>
                                <Button Name="btnClearFilters" Content="Очистить" 
                                      Style="{StaticResource SecondaryButton}"
                                      Width="80" Click="btnClearFilters_Click"/>
                            </StackPanel>
                        </Grid>
                    </GroupBox>
                    
                    <!-- Список объявлений -->
                    <DataGrid Grid.Row="1" Name="dgAllAdvertisements" 
                            AutoGenerateColumns="False" Margin="10"
                            CanUserAddRows="False" IsReadOnly="True"
                            GridLinesVisibility="Horizontal"
                            HeadersVisibility="Column"
                            SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding AdId}" Width="40"/>
                            <DataGridTextColumn Header="Дата" Binding="{Binding PublicationDate, StringFormat=dd.MM.yyyy}" Width="80"/>
                            <DataGridTextColumn Header="Город" Binding="{Binding City}" Width="100"/>
                            <DataGridTextColumn Header="Категория" Binding="{Binding CategoryName}" Width="120"/>
                            <DataGridTextColumn Header="Тип" Binding="{Binding TypeName}" Width="80"/>
                            <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="200"/>
                            <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat={}{0:N2} ₽}" Width="100"/>
                            <DataGridTextColumn Header="Автор" Binding="{Binding UserName}" Width="100"/>
                            <DataGridTemplateColumn Header="Действия" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Подробнее" 
                                              Style="{StaticResource SecondaryButton}"
                                              Click="btnViewDetails_Click"
                                              Tag="{Binding}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            
            <!-- Вкладка: Мои объявления -->
            <TabItem Header="Мои объявления" Name="tabMyAds">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Кнопка добавления -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
                        <Button Name="btnAddAd" Content="➕ Добавить объявление" 
                              Style="{StaticResource PrimaryButton}"
                              Width="200" Click="btnAddAd_Click"/>
                        <Button Name="btnRefreshMyAds" Content="🔄 Обновить" 
                              Style="{StaticResource SecondaryButton}"
                              Width="100" Click="btnRefreshMyAds_Click" Margin="10,0"/>
                    </StackPanel>
                    
                    <!-- Список моих объявлений -->
                    <DataGrid Grid.Row="1" Name="dgMyAdvertisements" 
                            AutoGenerateColumns="False" Margin="10"
                            CanUserAddRows="False" IsReadOnly="True"
                            GridLinesVisibility="Horizontal"
                            HeadersVisibility="Column"
                            SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding AdId}" Width="40"/>
                            <DataGridTextColumn Header="Дата" Binding="{Binding PublicationDate, StringFormat=dd.MM.yyyy}" Width="80"/>
                            <DataGridTextColumn Header="Город" Binding="{Binding City}" Width="100"/>
                            <DataGridTextColumn Header="Категория" Binding="{Binding CategoryName}" Width="120"/>
                            <DataGridTextColumn Header="Тип" Binding="{Binding TypeName}" Width="80"/>
                            <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="200"/>
                            <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat={}{0:N2} ₽}" Width="100"/>
                            <DataGridTextColumn Header="Статус" Binding="{Binding StatusName}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding StatusName}" Value="Активно">
                                                <Setter Property="Foreground" Value="Green"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding StatusName}" Value="Завершено">
                                                <Setter Property="Foreground" Value="Blue"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding StatusName}" Value="Приостановлено">
                                                <Setter Property="Foreground" Value="Orange"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding StatusName}" Value="Удалено">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTemplateColumn Header="Действия" Width="250">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="✏️" ToolTip="Редактировать"
                                                  Style="{StaticResource SecondaryButton}"
                                                  Width="30" Click="btnEditAd_Click"
                                                  Tag="{Binding}"/>
                                            <Button Content="✅" ToolTip="Завершить"
                                                  Style="{StaticResource SecondaryButton}"
                                                  Width="30" Click="btnCompleteAd_Click"
                                                  Tag="{Binding}"/>
                                            <Button Content="🗑️" ToolTip="Удалить"
                                                  Style="{StaticResource SecondaryButton}"
                                                  Width="30" Click="btnDeleteAd_Click"
                                                  Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- Панель информации о прибыли -->
                    <Border Grid.Row="2" Background="#F5F5F5" Padding="10" Margin="10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Общая прибыль от завершенных объявлений: " 
                                     Style="{StaticResource HeaderStyle}"/>
                            <TextBlock Name="txtTotalProfit" Text="0.00 ₽" 
                                     Style="{StaticResource HeaderStyle}"
                                     Foreground="#FF9C1A"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
            
            <!-- Вкладка: Завершенные сделки -->
            <TabItem Header="Завершенные сделки" Name="tabCompleted">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Список завершенных объявлений -->
                    <DataGrid Grid.Row="0" Name="dgCompletedAds" 
                            AutoGenerateColumns="False" Margin="10"
                            CanUserAddRows="False" IsReadOnly="True"
                            GridLinesVisibility="Horizontal"
                            HeadersVisibility="Column"
                            SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding AdId}" Width="40"/>
                            <DataGridTextColumn Header="Дата" Binding="{Binding PublicationDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Город" Binding="{Binding City}" Width="120"/>
                            <DataGridTextColumn Header="Категория" Binding="{Binding CategoryName}" Width="150"/>
                            <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="Сумма сделки" Binding="{Binding Price, StringFormat={}{0:N2} ₽}" Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="Green"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- Статистика -->
                    <Border Grid.Row="1" Background="#F5F5F5" Padding="10" Margin="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <TextBlock Text="Количество сделок: " Style="{StaticResource NormalText}"/>
                                <TextBlock Name="txtCompletedCount" Text="0" 
                                         Style="{StaticResource HeaderStyle}"
                                         Foreground="#FF9C1A"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <TextBlock Text="Общая сумма: " Style="{StaticResource NormalText}"/>
                                <TextBlock Name="txtCompletedSum" Text="0.00 ₽" 
                                         Style="{StaticResource HeaderStyle}"
                                         Foreground="#FF9C1A"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                <TextBlock Text="Средняя сумма сделки: " Style="{StaticResource NormalText}"/>
                                <TextBlock Name="txtAverageSum" Text="0.00 ₽" 
                                         Style="{StaticResource HeaderStyle}"
                                         Foreground="#FF9C1A"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
