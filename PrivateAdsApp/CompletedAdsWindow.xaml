<Window x:Class="PrivateAdsApp.Windows.CompletedAdsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Завершённые объявления" Height="650" Width="1100" WindowStartupLocation="CenterScreen"
        >
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок + общая прибыль -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <!--<Image Source="/Resources/logo.png" Width="60" Margin="0,0,15,0"/>-->
            <TextBlock Text="Завершённые объявления" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock x:Name="TxtTotalProfit" FontSize="22" FontWeight="Bold" Foreground="#FF4CAF50" 
                       Margin="40,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Список -->
        <ListView x:Name="LvCompleted" Grid.Row="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding Ad.PostDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                    <GridViewColumn Header="Заголовок" DisplayMemberBinding="{Binding Ad.Title}" Width="350"/>
                    <GridViewColumn Header="Цена" DisplayMemberBinding="{Binding Ad.Price, StringFormat={}{0:N0} руб.}" Width="120"/>
                    <GridViewColumn Header="Прибыль" DisplayMemberBinding="{Binding ProfitAmount, StringFormat={}{0:N0} руб.}" Width="130"/>
                    <GridViewColumn Header="Город" DisplayMemberBinding="{Binding Ad.City.CityName}" Width="120"/>
                    <GridViewColumn Header="Категория" DisplayMemberBinding="{Binding Ad.Category.CategoryName}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Кнопка Назад -->
        <Button Content="Назад" Grid.Row="2" Width="120" Height="40" HorizontalAlignment="Right" 
                Margin="0,20,0,0" Click="BtnBack_Click"/>
    </Grid>
</Window>